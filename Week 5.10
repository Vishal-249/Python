def count_words_of_length_at_least(S, L):
    words = S.split()
    count = sum(1 for word in words if len(word) >= L)
    return count

S = input().strip()
L = int(input())

print(count_words_of_length_at_least(S, L))      
           
           
